<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">
    <dubbo:application name="demo-provider"/>
    <!--<dubbo:registry address="multicast://224.5.6.7:1234" />-->
    <dubbo:protocol name="dubbo" port="20880"/>

    <dubbo:service interface="com.liupeng.dubbo.example.DemoService" ref="demoService" />
    <dubbo:monitor protocol="registry"/>
    <bean id="demoService" class="com.liupeng.dubbo.example.DemoService3Impl"/>
    <dubbo:registry address="zookeeper://127.0.0.1:2181"/>

    <bean id="genericService" class="com.liupeng.dubbo.example.MyGenericService"/>
    <dubbo:service interface="com.liupeng.dubbo.example.DemoService" ref="genericService"/>

    <!--分组聚合-->
    <!--<dubbo:service interface="com.liupeng.dubbo.example.DemoService" ref="demoService" group="333"/>-->

    <!--配置多个协议的时候会自动暴露多个exporter，在管理平台可以看到-->
    <!--<dubbo:protocol name="rmi" port="1099"/>-->

    <!--以下两种配置静态服务的方式均不起作用-->
    <!--<dubbo:registry address="zookeeper://127.0.0.1:2181?dynamic=false" />-->
    <!--<dubbo:registry address="zookeeper://127.0.0.1:2181" dynamic="false" />-->

    <!--不注册，只订阅（避免开发中的provider被调用）-->
    <!--<dubbo:registry address="zookeeper://127.0.0.1:2181" register="false"/>-->

    <!--不订阅，只注册（避免多个注册中心上的已注册服务不一致，例如A服务只在注册中心甲上注册了，而未在注册中心乙上注册）-->
    <!--<dubbo:registry id="hzRegistry" address="10.20.153.10:9090" subscribe="false"/>-->
</beans>
